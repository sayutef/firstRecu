import { ReplaceLettersPipe } from './replace-letters.pipe';

describe('ReplaceLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
